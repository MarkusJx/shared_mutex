{
  "name": "is-program-already-running",
  "version": "1.0.0",
  "description": "Check whether your program is already running",
  "main": "index.js",
  "scripts": {
    "install": "npm run-script build",
    "build": "cmake-js compile",
    "test": "node test.js",
    "pretest": "npm run-script build"
  },
  "author": "MarkusJx",
  "license": "MIT",
  "devDependencies": {
    "cmake-js": "^6.1.0",
    "node-addon-api": "^3.1.0"
  }
}
